install:
  - choco install ant
  - choco install ivy
  - SET PATH=C:\Ruby22\bin;%PATH%
  - ps: refreshenv
  - gem install os
  - ruby --version
  - rake --version
  - java -version
  - ant -version

before_test:
  - rake dependencies
  - rake

test_script:
  - rake test

build: off
