install:
  - choco install ant
  - SET PATH=C:\Ruby22\bin;%PATH%
  - ps: refreshenv
  - gem install os
  - ruby --version
  - rake --version
  - java -version
  - ant -version

script:
  - rake dependencies
  - rake

test_script:
  - rake test

build: off
