machine:
  ruby:
    version: 2.4

#dependencies:
  #override:
    #- gem install os
    #- ruby --version
    #- rake --version
    #- java -version
    #- ant -version

#test:
  #override:
    #- rake
    #- rake test
    #- rake unittest
