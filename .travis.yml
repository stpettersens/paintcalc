notifications:
  email: false

sudo: required
language: java

before_install:
  - sudo apt-get install -y rubygems
  - sudo gem install os
  - ruby --version
  - rake --version
  - java -version
  - ant -version

script:
  - rake
